웹DB 
로컬DB?


1. 로컬에서 csv 파일 존재. 원래 센서에서 1초마다 DB로 전달받는 과정이어야 하지만, 데이터가 없으니 csv로 대체.
*실제 환경에서는 센서데이터가 1초마다 DB 적재. 그러나 지금은 3년치 데이터가 누적된 csv 파일을 기반으로 데이터 적재 예정. 
<기능>
-test/ops 모드 구분? : test 모드에서는 특정 csv 파일 전체를 DB로 적재. ops 모드에서는 실시간 DB 적재. 
-


2. 1초 단위 데이터들을 비교하여 데이터 이상치가 있는지 판단. 
<기능>
?

AI 태우는 과정을 할 듯? 

3. MCP에 (2번의 데이터 이상치 + ?) 를 입력하고, 메뉴열(로컬 pc에 들어있음)을 참조하여 사용자 행동지침을 출력

4. 웹에서 2번에서 받은 이상치 데이터와 3번의 결과(정확히 무엇을 의미할까?)를 받아 보여줌. 
-보여주는 형태는 스택형식. 
- 이상치를 받았다면 alert 
