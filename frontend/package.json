{
  "name": "alert-dashboard",
  "version": "1.0.0",
  "description": "Alert Dashboard 웹 UI 프로토타입 - Vue.js 버전",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:dashboard-api": "npm run build && start-server-and-test \"uvicorn app.main:app --app-dir ../backend\" http://127.0.0.1:8000/dashboard/send \"vitest run tests/dashboard.api.test.js\""
  },
  "dependencies": {
    "start-server-and-test": "^2.1.3",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
